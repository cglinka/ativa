%h2= @post.title
%p views/projects/posts/_show.html.haml
%p= @post.message
- if @post.attachment_url.blank?
  no attachment available
-else
  %iframe{height: "800", width:"100%", frameBorder: "0", src: @post.attachment_url}